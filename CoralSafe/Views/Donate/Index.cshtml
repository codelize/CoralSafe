@model IEnumerable<CoralSafe.Models.Donate>
<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gerenciar Doações</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center mb-4">Doações</h1>
        <table class="table table-hover table-striped">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">Valor Doado</th>
                    <th scope="col">Data da Doação</th>
                    <th scope="col">ID do Doador</th>
                    <th scope="col">Nome do Doador</th>
                    <th scope="col">Ações</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var donate in Model)
                {
                    <tr>
                        <td>@Html.DisplayFor(modelItem => donate.Valor)</td>
                        <td>@Html.DisplayFor(modelItem => donate.DataDonate)</td>
                        <td>@Html.DisplayFor(modelItem => donate.idUserDonate)</td>
                        <td>@Html.DisplayFor(modelItem => donate.User.name)</td> 
                        <td>
                            <a role="button" class="btn btn-warning btn-sm m-1" asp-route-id="@donate.Id" asp-controller="Donate" asp-action="Editar">
                                <i class="fas fa-edit"></i> Editar
                            </a>
                            <a role="button" class="btn btn-danger btn-sm m-1" asp-route-id="@donate.Id" asp-controller="Donate" asp-action="Deletar">
                                <i class="fas fa-trash-alt"></i> Apagar
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
            <a class="btn btn-primary" asp-controller="Donate" asp-action="Criar">
                <i class="fas fa-plus-circle"></i> Adicionar nova doação
            </a>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>